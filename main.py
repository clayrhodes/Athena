from config.settings import APP_NAME
from config.settings import VERSION
from config.settings import DEFAULT_SYMBOL

print("=" * 40)
print(APP_NAME)
print(f"Version: {VERSION}")
print(f"Default Market: {DEFAULT_SYMBOL}")
print("=" * 40)